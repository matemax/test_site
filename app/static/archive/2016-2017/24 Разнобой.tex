\documentclass[14pt,DIV14]{scrartcl}
\usepackage[cp1251]{inputenc}
\usepackage[russian]{babel}
%\usepackage[utf8]{inputenc}
\usepackage{sheet}
\usepackage{multicol}
 \usepackage{epigraph}
  \usepackage{graphicx}
%\usepackage{xfrac}
\usepackage{a4wide}
\pagestyle{empty}
\usepackage{tikz}
\usepackage{graphicx,epstopdf}
\def\mod_#1 {\mathrel{\mathop{\equiv}\limits_{#1}}}
\newtheorem{theorem}{“еорема}
\evensidemargin=-1cm \oddsidemargin=-1cm \textwidth=18cm
\topmargin=-2cm \textheight=26cm

\begin{document}
\sheet{ћалый механико-математический факультет, 13-02.}{–азнобой
}{11 марта 2017 9-11 класс}


\begin{task}
Let $P(x) = a_nx^n +\ldots+a_0$ be a complex polynomial with $a_n \ne 0$ and $M = \max\limits_{0\leqslant k<n}\left|\dfrac{a_k}{a_n}\right|$.
If $a_{n-1} = \ldots = a_{n-k+1} = 0$, then all roots of the polynomial $P$ are less than $1+
\sqrt[k]{M}$ in modulus.
In particular, for $k = 1$, each zero of $P(x)$ is of modulus less than $M +1$.
\end{task}


\begin{task}
—уществует ли ограниченна€ функци€ $f: \mathbb{R} \to \mathbb{R}$ така€, что $f(1) >  0$ и $f(x)$ удовлетвор€ет при всех $x, y \in \mathbb{R}$ неравенству
$
f^2(x + y) \geqslant f^2(x)+2f(xy)+ f^2(y)$
?
\end{task}

\begin{task}
ѕоследовательности положительных чисел $(x_n)$ и $(y_n)$ удовлетвор€ют услови€м $x_{n+2} = x_n +x^2_{n+1}, y_{n+2} = y^2_n +y_{n+1}$ при всех натуральных $n$. ƒокажите, что если все числа $x_1$, $x_2$, $y_1$, $y_2$ больше 1, то $x_n >y_n$ при
каком-нибудьнатуральном n.
\end{task}

\begin{task}
$a$ и $b$~--- различные натуральные числа такие, что $ab(a + b)$ делитс€на $a^2 + ab + b^2$. ƒокажите, что $|a - b| > \sqrt[3]{ab}$.
\end{task}

\begin{task}
ѕусть $A'$~--- точка касани€ вневписанной окружности треугольника
$ABC$ со стороной $BC$. ѕр€ма€ $a$ проходит через точку $A'$ и параллельна биссектрисе внутреннего угла $A$. јналогично стро€тс€ пр€мые $b$ и $c$. ƒокажите, что $a, b$ и $c$ пересекаютс€ в одной точке.
\end{task}
\end{document}
\begin{definition}
\end{definition}
\begin{task}
\end{task}
\begin{axiom}
\end{axiom}
\begin{assertion}
\end{assertion}
\begin{exercise}
\end{exercise}
