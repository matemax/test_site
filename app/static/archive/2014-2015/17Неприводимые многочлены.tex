\documentclass[14pt,DIV14]{scrartcl}
\usepackage[cp1251]{inputenc}
\usepackage[russian]{babel}
%\usepackage[utf8]{inputenc}
\usepackage{sheet}
\usepackage{multicol}
 \usepackage{epigraph}
  \usepackage{graphicx}
%\usepackage{xfrac}
\usepackage{a4wide}
\pagestyle{empty}
\def\mod_#1 {\mathrel{\mathop{\equiv}\limits_{#1}}}
\newtheorem{theorem}{Теорема}
\evensidemargin=-1cm \oddsidemargin=-1cm \textwidth=18cm
\topmargin=-2cm \textheight=26cm

\begin{document}
\sheet{Малый механико-математический факультет, 13-02.}{Неприводимость многочленов
}{14 февраля 2015 9-11 класс}
\begin{definition}
Пусть $K$~--- коммутативное кольцо, многочлен $f\in K\{x_1,\ldots,x_n\}$ называется неприводимым, если  из представления $f=hg$ следует, что один из многочленов $g$ ил $h$ константа.
\end{definition}

\begin{theorem}
Пусть $K$~--- поле, тогда любой  многочлен из $f\in K\{x_1,\ldots,x_n\}$ единственным образом расскладывается в произведение неприводимых  многочленов $f=f_1f_2\ldots f_k$ с точностью до перестановки и домножения на константы.
\end{theorem}
\begin{task}Найдите все неприводимые многочлены не выше, чем четвёртой степени над полем $\mathbb{Z}_2$. (Маленькая подсказка: а сколько всего многочленов не выше четвёртой степени существует по модулю 2?).
\end{task}



\begin{definition}
Пусть $f\in\mathbb{Z}[x]$, тогда обозначим за $c(f)$~--- наибольший общий делитель всех коэффициентов $f$.
\end{definition}


\begin{task}\sub Пусть $c(f)=1$ и $c(g)=1$. Докажите, что $c(fg)=1$.({\it Если  $c(fg)\ne 1$, то у него есть простой делитель})\\
\sub  Докажите, что $c(fg)=c(f)c(g)$.\\
\sub {\bf Лемма Гауса}  Докажите, что многочлен $f\in\mathbb{Z}[x]$ неприводим над $\mathbb{Z}$ тогда и только тогда, когда он неприводим над $\mathbb{Q}$
\end{task}


\begin{task}[Критерий Эйзенштейна] Пусть $f\in\mathbb{Z}[x]$ и $f=a_nx^n+\ldots+a_0$. Известно, что $a_n\not\!\vdots p$, $a_{n-1},\ldots a_0, \vdots p$ и $a_0\not\!\vdots p^2$ для некоторого простого $p$. Докажите, что многочлен $f$ неприводим над $\mathbb{Z}$
\end{task}

\begin{task}
Пусть $p$~--- простое число. Докажите, что многочлен $x^{p-1}+x^{p-2}+\ldots+x+1$ неприводим над $\mathbb{Z}$
\end{task}


\begin{task}	Докажите, что если $p/q$~--- несократимая дробь, являющаяся корнем полинома $f(x)=a_nx^n+\ldots+a_1x+a_0$ с целыми коэффициентами, то $qm-p$ делит $f(m)$ для любого целого $m$.
\end{task}




\begin{task}Доказать, что многочлен $(x-a_1)(x-a_2)\ldots(x-a_n) - 1$ неприводим, если $a_1, a_2, \ldots, a_n$~--- различные целые числа.
\end{task}
\begin{task}
Докажите, что любой многочлен из $\mathbb{Q}[x]$ представляется в виде суммы двух неприводимых многочленов.
\end{task}


\begin{task}
\sub Пусть $g\in K[x]$~--- неприводимый многочлен над полем $K$. Докажите, что на остатках по модулю многочлена $g$ можно ввести операции сложения и умножения так, чтобы они образовывали поле. Для многочленов \sub $x^2+1\in \mathbb{R}[x]$;\sub $x^2-2\in\mathbb{Q}[x]$  постройте биективные отображения $f$ из поля остатков этих многочленов  в $\mathbb{C}$ и в $\mathbb{Q}[\sqrt{2}]$ соответственно так, чтобы для любых двух остатков $a,b$ выполнялось:
\begin{enumerate}
\item $f(a+b)=f(a)+f(b)$;
\item $f(ab)=f(a)f(b)$
\end{enumerate}
\end{task}

\newpage  {\bf Алгоритм Кронекера.} 

Алгоритм говорит про заданный многочлен $f\in \mathbb{Z}$, является ли он неприводимым. Если не является, то строит многочлен $g\in \mathbb{Z}, \deg g >0$, который является делителем $f$. Докажите корректность алгоритма.

1) Пусть $\deg f = n$ и $r=[\dfrac{n}{2}]$.\\
2) Рассмотрим числа $c_j=f(j), j=0,\ldots, r$. Если хотябы одно из чисел $c_j=0$, алгоритм прекращает свою работу, $g=x-j$.\\
3) Если все $c_j$ отличны от 0. Для каждого $0\leqslant j\leqslant r$ Строим множество $C_j$, состоящее из всех делителей числа $c_j$.\\
4) Рассматриваем всевозможные различные наборы чисел $d= d_0,\ldots, d_r$ таких, что $d_i\in C_i$.\\
5) Для каждого такого  набора строим интерполяционный многочлен $g_d$ по точкам $0,1,\ldots,r$ в которых многочлен принимает значения $d_0,\ldots, d_r$.\\
6) Проверяем, делится ли $f$ на $g_d$. Если  разделился на какой-то, то алгоритм заканчивает свою работу и возвращает $g=g_d$.\\
7) Если ни для какого набора $d= d_0,\ldots, d_r$ многочлен $f$ не разделился на $g_d$, то $f$  неприводим. Алгоритм завершает свою работу.
\end{document}
\begin{definition}
\end{definition}
\begin{task}
\end{task}
\begin{axiom}
\end{axiom}
\begin{assertion}
\end{assertion}
\begin{exercise}
\end{exercise}
